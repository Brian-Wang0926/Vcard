# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# 忽略.env文件
client/.env
server/.env

# 忽略node_modules目录（通常前端和后端都有）
**node_modules/

# 忽略构建目录（如果前端和后端都有）
build/

# 忽略测试覆盖率报告目录（如果前端和后端都有）
coverage/

# 忽略一些常见的编辑器或操作系统生成的文件
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# 忽略npm和yarn的日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
